{
    "name": "blind_signatures-snarkjs-prover-engine",
    "version": "1.0.0",
    "description": "A local web service to run snarkjs operations for the Blind Signatures Library.",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "circomlib": "^2.0.5",
        "circomlibjs": "^0.1.7",
        "express": "^4.19.2",
        "snarkjs": "^0.7.5"
    }
}
